# ─── Shopify App Credentials ───────────────────────────────────────────────
# Get these from your Shopify Partner Dashboard → App → API credentials
SHOPIFY_API_KEY="b66bdbd291bc5b4896d3dea98c0fbf2f"
SHOPIFY_API_SECRET="your_shopify_api_secret"
# The public URL where this app is hosted (no trailing slash)
# During dev: shopify CLI auto-sets this. For production: your Render URL.
SHOPIFY_APP_URL="https://your-app.onrender.com"

# Comma-separated list of OAuth scopes
SCOPES="read_products,write_products,read_orders,read_customers"
# ─── Database ────────────────────────────────────────────────────────────────
# PostgreSQL connection string (Render provides this automatically)
DATABASE_URL="postgresql://dovr_user:I2uuKnifK8lm59e3LFS78Tmxqs5GzuwZ@dpg-d7hkpu8sfn5c73eb1jsg-a.oregon-postgres.render.com/dovr"
# ─── Email (SMTP) ────────────────────────────────────────────────────────────
# SMTP credentials for sending support emails
# Gmail example: use an App Password (not your regular password)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@yourdomain.com"
SMTP_PASS="your_app_password"

# Where support emails are sent TO (your agency inbox)
SUPPORT_EMAIL="support@youragency.com"

# ─── Admin Panel ─────────────────────────────────────────────────────────────
# Secret key to protect the /app/admin route
# Change this to something strong before deploying!
ADMIN_SECRET="change-me-before-deploy"
